{
  "compilerOptions": {
    // require strict type checking (null-save)
    "strict": true,
    // tell typescript to generate ESM syntax
    "target": "ESNext",
    // tell typescript to require ESM syntax as input (including .js file imports)
    "module": "NodeNext",
    // define where to output the compiled code
    "outDir": "../../dist-electron",
    // ignore errors from dependencies
    "skipLibCheck": true,
    // add global types
    "types": ["../../types"]
  }
}
